//

var schema = {
  id: '' // result > extractorData > data[i] > group[0] > Vin value > text
  title: '', // result > extractorData > data[i] > group[0] > Media image[0] > alt
  url: 'String', // result > extractorData > url
  imageSrc: 'String', // result > extractorData > data[i] > group[0] > Media image[0]
  color: 'String', // result > extractorData > data[i] > group[0] > Exterior value[0] > text
}
